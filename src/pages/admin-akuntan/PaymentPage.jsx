import { useEffect,useMemo,useState } from "react";
import { Check,Eye,Plus,XCircle } from "lucide-react";
import PurchaseOrderService from "../../services/PurchaseOrderService";
import PaymentService from "../../services/PaymentService";
const labels={draft:"Draft",submitted:"Menunggu Konfirmasi",paid:"Dibayar",rejected:"Ditolak"};
function PaymentPage(){
 const [orders,setOrders]=useState([]),[payments,setPayments]=useState([]),[selected,setSelected]=useState(null),[show,setShow]=useState(false),[search,setSearch]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),[form,setForm]=useState({purchaseOrderId:"",paymentNumber:"",amount:"",paymentMethod:"bank_transfer",paymentDate:new Date().toISOString().slice(0,10),notes:""});
 const load=async()=>{try{const o=await PurchaseOrderService.getAll();const os=Array.isArray(o)?o:[];setOrders(os);const p=[];for(const order of os){try{const data=await PaymentService.getByPurchaseOrder(order.purchase_order_id);(Array.isArray(data)?data:[]).forEach(x=>p.push({...x,purchaseOrder:order}))}catch{}}setPayments(p)}catch(e){setError(e.message)}};
 useEffect(()=>{load()},[]);
 const filtered=useMemo(()=>{const q=search.toLowerCase();return payments.filter(p=>`${p.payment_number||""} ${p.status||""} ${p.purchaseOrder?.po_number||""}`.toLowerCase().includes(q))},[payments,search]);
 const detail=async p=>{try{const d=await PaymentService.getById(p.payment_id);setSelected(d?.data||d)}catch(e){setError(e.message)}};
 const action=async(p,type)=>{try{if(type==="submit")await PaymentService.submit(p.payment_id);if(type==="confirm")await PaymentService.confirm(p.payment_id);if(type==="reject")await PaymentService.reject(p.payment_id);await load();setSelected(null)}catch(e){setError(e?.data?.message||e.message)}};
 const create=async e=>{e.preventDefault();setLoading(true);try{await PaymentService.create(form.purchaseOrderId,{payment_number:form.paymentNumber.trim(),amount:Number(form.amount),payment_method:form.paymentMethod,payment_date:form.paymentDate,notes:form.notes||null});setShow(false);await load()}catch(e){setError(e?.data?.message||e.message)}finally{setLoading(false)}};
 if(selected)return <section className="space-y-5"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-blue-600">Payment</p><h1 className="mt-1 text-2xl font-bold">{selected.payment_number}</h1></div><button onClick={()=>setSelected(null)} className="rounded-lg border px-4 py-2 text-sm">Kembali</button></div>{error&&<div className="rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="rounded-xl border bg-white p-5 shadow-sm"><div className="grid gap-5 md:grid-cols-3"><div><p className="text-xs text-slate-500">No. PO</p><p className="mt-1 text-sm">{selected.payment_purchase_order?.po_number||"-"}</p></div><div><p className="text-xs text-slate-500">Nominal</p><p className="mt-1 text-sm font-bold">Rp {Number(selected.amount||0).toLocaleString("id-ID")}</p></div><div><p className="text-xs text-slate-500">Status</p><p className="mt-1 text-sm font-semibold">{labels[selected.status]||selected.status}</p></div><div><p className="text-xs text-slate-500">Metode</p><p className="mt-1 text-sm">{selected.payment_method||"-"}</p></div><div><p className="text-xs text-slate-500">Tanggal</p><p className="mt-1 text-sm">{selected.payment_date||"-"}</p></div><div><p className="text-xs text-slate-500">Catatan</p><p className="mt-1 text-sm">{selected.notes||"-"}</p></div></div><div className="mt-6 flex gap-2">{selected.status==="draft"&&<button onClick={()=>action(selected,"submit")} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Kirim Konfirmasi</button>}{selected.status==="submitted"&&<><button onClick={()=>action(selected,"confirm")} className="inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-2 text-sm font-semibold text-white"><Check size={15}/>Konfirmasi</button><button onClick={()=>action(selected,"reject")} className="inline-flex items-center gap-2 rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white"><XCircle size={15}/>Tolak</button></>}</div></div></section>;
 return <section><div className="mb-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-blue-600">Accounting</p><h1 className="mt-1 text-2xl font-bold">Payment</h1><p className="mt-1 text-sm text-slate-500">Kelola dan konfirmasi pembayaran purchase order.</p></div><button onClick={()=>setShow(true)} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>Buat Payment</button></div>{error&&<div className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="rounded-xl border bg-white shadow-sm"><div className="border-b p-4"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Cari payment atau PO..." className="w-full max-w-md rounded-lg border px-3 py-2.5 text-sm"/></div><div className="overflow-x-auto"><table className="w-full min-w-[750px] text-left"><thead className="bg-slate-50"><tr><th className="p-4 text-xs">No</th><th className="p-4 text-xs">Payment</th><th className="p-4 text-xs">PO</th><th className="p-4 text-right text-xs">Nominal</th><th className="p-4 text-xs">Status</th><th className="p-4 text-center text-xs">Aksi</th></tr></thead><tbody>{filtered.map((p,i)=><tr key={p.payment_id} className="border-t"><td className="p-4 text-sm">{i+1}</td><td className="p-4 text-sm font-semibold">{p.payment_number}</td><td className="p-4 text-sm">{p.purchaseOrder?.po_number||"-"}</td><td className="p-4 text-right text-sm">Rp {Number(p.amount||0).toLocaleString("id-ID")}</td><td className="p-4 text-sm">{labels[p.status]||p.status}</td><td className="p-4 text-center"><button onClick={()=>detail(p)} className="inline-flex items-center gap-1 rounded-lg bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-600"><Eye size={14}/>Detail</button></td></tr>)}{!filtered.length&&<tr><td colSpan="6" className="p-10 text-center text-sm text-slate-400">Belum ada payment.</td></tr>}</tbody></table></div></div>{show&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><form onSubmit={create} className="w-full max-w-lg rounded-2xl bg-white p-6"><h2 className="text-lg font-bold">Buat Payment</h2><div className="mt-5 space-y-4"><select required value={form.purchaseOrderId} onChange={e=>setForm({...form,purchaseOrderId:e.target.value})} className="w-full rounded-lg border bg-white px-3 py-2.5 text-sm"><option value="">Pilih PO</option>{orders.filter(o=>!['completed','cancelled','failed'].includes(o.status)).map(o=><option key={o.purchase_order_id} value={o.purchase_order_id}>{o.po_number} — Rp {Number(o.total||0).toLocaleString('id-ID')}</option>)}</select><input required placeholder="No. Payment" value={form.paymentNumber} onChange={e=>setForm({...form,paymentNumber:e.target.value})} className="w-full rounded-lg border px-3 py-2.5 text-sm"/><input required min="0" type="number" placeholder="Nominal" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} className="w-full rounded-lg border px-3 py-2.5 text-sm"/><div className="grid gap-4 sm:grid-cols-2"><select value={form.paymentMethod} onChange={e=>setForm({...form,paymentMethod:e.target.value})} className="rounded-lg border bg-white px-3 py-2.5 text-sm"><option value="cash">Cash</option><option value="bank_transfer">Bank Transfer</option><option value="ewallet">E-Wallet</option></select><input required type="date" value={form.paymentDate} onChange={e=>setForm({...form,paymentDate:e.target.value})} className="rounded-lg border px-3 py-2.5 text-sm"/></div><textarea placeholder="Catatan" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})} className="min-h-20 w-full rounded-lg border px-3 py-2.5 text-sm"/></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={()=>setShow(false)} className="rounded-lg border px-4 py-2 text-sm">Batal</button><button disabled={loading} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">{loading?'Menyimpan...':'Simpan Payment'}</button></div></form></div>}</section>;
}
export default PaymentPage;
