import { useEffect, useMemo, useState } from "react";
import { Check, Eye, Plus, XCircle } from "lucide-react";
import PurchaseOrderService from "../../services/PurchaseOrderService";
import PaymentService from "../../services/PaymentService";

const labels = { draft: "Draft", submitted: "Menunggu Konfirmasi", paid: "Dibayar", rejected: "Ditolak" };

function PaymentPage() {
  const [orders, setOrders] = useState([]), [payments, setPayments] = useState([]), [selected, setSelected] = useState(null), [show, setShow] = useState(false), [search, setSearch] = useState(""), [error, setError] = useState(""), [loading, setLoading] = useState(false);
  const [form, setForm] = useState({ purchaseOrderId: "", paymentNumber: "", amount: "", paymentMethod: "bank_transfer", paymentDate: new Date().toISOString().slice(0, 10), notes: "" });

  const load = async () => { try { const result = await PurchaseOrderService.getAll(); const orderList = Array.isArray(result) ? result : []; setOrders(orderList); const list = []; for (const order of orderList) { try { const data = await PaymentService.getByPurchaseOrder(order.purchase_order_id); (Array.isArray(data) ? data : []).forEach((payment) => list.push({ ...payment, purchaseOrder: order })); } catch { /* PO tanpa payment */ } } setPayments(list); } catch (e) { setError(e.message); } };
  useEffect(() => {
    const loadTimer = window.setTimeout(load, 0);
    return () => window.clearTimeout(loadTimer);
  }, []);
  const filtered = useMemo(() => { const query = search.toLowerCase(); return payments.filter((payment) => `${payment.payment_number || ""} ${payment.status || ""} ${payment.purchaseOrder?.po_number || ""}`.toLowerCase().includes(query)); }, [payments, search]);
  const detail = async (payment) => { try { const data = await PaymentService.getById(payment.payment_id); setSelected(data?.data || data); } catch (e) { setError(e.message); } };
  const action = async (payment, type) => { try { if (type === "submit") await PaymentService.submit(payment.payment_id); if (type === "confirm") await PaymentService.confirm(payment.payment_id); if (type === "reject") await PaymentService.reject(payment.payment_id); await load(); setSelected(null); } catch (e) { setError(e?.data?.message || e.message); } };
  const create = async (event) => { event.preventDefault(); setLoading(true); try { await PaymentService.create(form.purchaseOrderId, { payment_number: form.paymentNumber.trim(), amount: Number(form.amount), payment_method: form.paymentMethod, payment_date: form.paymentDate, notes: form.notes || null }); setShow(false); await load(); } catch (e) { setError(e?.data?.message || e.message); } finally { setLoading(false); } };

  if (selected) return <section className="space-y-5"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-blue-600">Pembayaran</p><h1 className="mt-1 text-2xl font-bold">{selected.payment_number}</h1></div><button onClick={() => setSelected(null)} className="rounded-lg border border-slate-200 px-4 py-2 text-sm">Kembali</button></div>{error && <ErrorMessage>{error}</ErrorMessage>}<div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-5 md:grid-cols-3"><Info label="No. PO" value={selected.payment_purchase_order?.po_number || "-"} /><Info label="Nominal" value={`Rp ${Number(selected.amount || 0).toLocaleString("id-ID")}`} strong /><Info label="Status" value={labels[selected.status] || selected.status} strong /><Info label="Metode" value={selected.payment_method || "-"} /><Info label="Tanggal" value={selected.payment_date || "-"} /><Info label="Catatan" value={selected.notes || "-"} /></div><div className="mt-6 flex gap-2">{selected.status === "draft" && <button onClick={() => action(selected, "submit")} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Kirim Konfirmasi</button>}{selected.status === "submitted" && <><button onClick={() => action(selected, "confirm")} className="inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-2 text-sm font-semibold text-white"><Check size={15} />Konfirmasi</button><button onClick={() => action(selected, "reject")} className="inline-flex items-center gap-2 rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white"><XCircle size={15} />Tolak</button></>}</div></div></section>;

  return <section><div className="mb-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-blue-600">Keuangan</p><h1 className="mt-1 text-2xl font-bold">Pembayaran</h1><p className="mt-1 text-sm text-slate-500">Kelola dan konfirmasi pembayaran purchase order.</p></div><button onClick={() => setShow(true)} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17} />Buat Pembayaran</button></div>{error && <ErrorMessage>{error}</ErrorMessage>}<div className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 p-4"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Cari pembayaran atau PO..." className="w-full max-w-md rounded-lg border border-slate-200 px-3 py-2.5 text-sm" /></div><div className="overflow-x-auto"><table className="w-full min-w-[750px] text-left"><thead className="bg-slate-50"><tr>{["No", "Pembayaran", "PO", "Nominal", "Status", "Aksi"].map((title) => <th key={title} className="p-4">{title}</th>)}</tr></thead><tbody>{filtered.map((payment, index) => <tr key={payment.payment_id} className="border-t border-slate-100"><td className="p-4 text-sm">{index + 1}</td><td className="p-4 text-sm font-semibold">{payment.payment_number}</td><td className="p-4 text-sm">{payment.purchaseOrder?.po_number || "-"}</td><td className="p-4 text-right text-sm">Rp {Number(payment.amount || 0).toLocaleString("id-ID")}</td><td className="p-4 text-sm">{labels[payment.status] || payment.status}</td><td className="p-4 text-center"><button onClick={() => detail(payment)} className="inline-flex items-center gap-1 rounded-lg bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-600"><Eye size={14} />Detail</button></td></tr>)}{!filtered.length && <tr><td colSpan="6" className="p-10 text-center text-sm text-slate-400">Belum ada pembayaran.</td></tr>}</tbody></table></div></div>{show && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><form onSubmit={create} className="w-full max-w-lg rounded-2xl bg-white p-6"><h2 className="text-lg font-bold">Buat Pembayaran</h2><div className="mt-5 space-y-4"><select required value={form.purchaseOrderId} onChange={(event) => setForm({ ...form, purchaseOrderId: event.target.value })} className="w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm"><option value="">Pilih PO</option>{orders.filter((order) => !["completed", "cancelled", "failed"].includes(order.status)).map((order) => <option key={order.purchase_order_id} value={order.purchase_order_id}>{order.po_number} — Rp {Number(order.total || 0).toLocaleString("id-ID")}</option>)}</select><input required placeholder="No. Pembayaran" value={form.paymentNumber} onChange={(event) => setForm({ ...form, paymentNumber: event.target.value })} className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm" /><input required min="0" type="number" placeholder="Nominal" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm" /><div className="grid gap-4 sm:grid-cols-2"><select value={form.paymentMethod} onChange={(event) => setForm({ ...form, paymentMethod: event.target.value })} className="rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm"><option value="cash">Cash</option><option value="bank_transfer">Bank Transfer</option><option value="ewallet">E-Wallet</option></select><input required type="date" value={form.paymentDate} onChange={(event) => setForm({ ...form, paymentDate: event.target.value })} className="rounded-lg border border-slate-200 px-3 py-2.5 text-sm" /></div><textarea placeholder="Catatan" value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} className="min-h-20 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm" /></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={() => setShow(false)} className="rounded-lg border border-slate-200 px-4 py-2 text-sm">Batal</button><button disabled={loading} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">{loading ? "Menyimpan..." : "Simpan Pembayaran"}</button></div></form></div>}</section>;
}

function Info({ label, value, strong = false }) { return <div><p className="text-xs text-slate-500">{label}</p><p className={`mt-1 text-sm ${strong ? "font-semibold" : ""}`}>{value}</p></div>; }
function ErrorMessage({ children }) { return <div className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{children}</div>; }

export default PaymentPage;
